export const usePortalLogoBar = () => {
    // Mock data for now; replace with dynamic data when available
    const title = "ZALK: Advanced Learning and Knowledge";
    const userInfo = {
        profileImage: null,
    };

    return {
        title,
        userInfo,
    };
};